<mah:MetroWindow x:Class="ExampleInterface.Views.SetupWizardView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                 xmlns:views="clr-namespace:ExampleInterface.Views"
                 xmlns:conv="clr-namespace:ExampleInterface.Converters"
                 Title="{Binding WindowTitle}"
                 TitleCharacterCasing="Normal"
                 Height="680" Width="960"
                 MinHeight="560" MinWidth="840"
                 WindowStartupLocation="CenterScreen"
                 GlowBrush="{DynamicResource AccentColorBrush}"
                 BorderThickness="1"
                 Background="Transparent"
                 ResizeMode="CanMinimize"
                 ShowIconOnTitleBar="False">
  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Dark.Red.xaml" />
        <ResourceDictionary Source="pack://application:,,,/ExampleInterface;component/Themes/InstallerResources.xaml" />
      </ResourceDictionary.MergedDictionaries>
      <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
      <conv:EnumToVisibilityConverter x:Key="PageVisibility" />
    </ResourceDictionary>
  </Window.Resources>

  <Grid Background="{StaticResource Installer.PanelBackgroundBrush}"
        TextElement.FontFamily="{StaticResource Installer.FontFamily}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Header -->
    <Border Grid.Row="0" Background="{StaticResource Installer.HeaderBackgroundBrush}" Padding="24,20">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Width="52" Height="52" CornerRadius="12" Background="#33FFFFFF" Margin="0,0,16,0">
          <iconPacks:PackIconMaterial Kind="PackageVariantClosed"
                                      Width="28" Height="28"
                                      Foreground="White"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center" />
        </Border>

        <StackPanel Grid.Column="1" VerticalAlignment="Center">
          <TextBlock Text="{Binding ProductName}" FontSize="22" FontWeight="SemiBold"
                     Foreground="{StaticResource Installer.HeaderForegroundBrush}" />
          <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
            <TextBlock Text="{Binding ProductVersion, StringFormat=Version {0}}"
                       Foreground="{StaticResource Installer.SubtleForegroundBrush}" FontSize="13" />
            <TextBlock Text=" · " Foreground="{StaticResource Installer.SubtleForegroundBrush}" FontSize="13" />
            <TextBlock Text="{Binding Manufacturer}"
                       Foreground="{StaticResource Installer.SubtleForegroundBrush}" FontSize="13" />
          </StackPanel>
          <TextBlock Text="{Binding HeaderText}" Margin="0,10,0,0" FontSize="15" FontWeight="SemiBold"
                     Foreground="White" TextWrapping="Wrap" />
          <TextBlock Text="{Binding DescriptionText}" Margin="0,4,0,0" FontSize="13"
                     Foreground="{StaticResource Installer.SubtleForegroundBrush}" TextWrapping="Wrap" />
        </StackPanel>
      </Grid>
    </Border>

    <!-- Configuration (fresh install) -->
    <Grid Grid.Row="1"
          Visibility="{Binding ShowConfigurationNavigation, Converter={StaticResource BoolToVisibility}}">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="220" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Border Grid.Column="0" Background="#66140A0A"
              BorderBrush="{StaticResource Installer.CardBorderBrush}" BorderThickness="0,0,1,0"
              Padding="16,20">
        <StackPanel>
          <TextBlock Text="Configuration" FontSize="12" FontWeight="SemiBold"
                     Foreground="{StaticResource Installer.MutedTextBrush}" Margin="0,0,0,12" />
          <ListBox ItemsSource="{Binding NavigationItems}"
                   SelectedItem="{Binding SelectedNavigationItem}"
                   Background="Transparent"
                   BorderThickness="0"
                   Padding="0"
                   ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                   ItemContainerStyle="{StaticResource Installer.NavigationItem}">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <StackPanel Orientation="Horizontal">
                  <iconPacks:PackIconMaterial Kind="{Binding IconKind}" Width="18" Height="18"
                                              VerticalAlignment="Center" Margin="0,0,10,0"
                                              Foreground="{StaticResource Installer.SectionForegroundBrush}" />
                  <TextBlock Text="{Binding Title}" VerticalAlignment="Center"
                             Foreground="{StaticResource Installer.SectionForegroundBrush}" />
                </StackPanel>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </StackPanel>
      </Border>

      <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Padding="24,20">
        <Grid DataContext="{Binding}">
          <views:FeaturesPageView
            Visibility="{Binding SelectedPage, Converter={StaticResource PageVisibility}, ConverterParameter=Features}" />
          <Grid Visibility="{Binding SelectedPage, Converter={StaticResource PageVisibility}, ConverterParameter=Database}">
            <views:DatabaseConnectionPageView DataContext="{Binding DatabaseConnection}" />
          </Grid>
          <views:DiagnosticsPageView
            Visibility="{Binding SelectedPage, Converter={StaticResource PageVisibility}, ConverterParameter=Diagnostics}" />
        </Grid>
      </ScrollViewer>
    </Grid>

    <!-- Modify (feature selection only) -->
    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,20"
                  Visibility="{Binding ShowModifyContent, Converter={StaticResource BoolToVisibility}}">
      <views:FeaturesPageView DataContext="{Binding}" />
    </ScrollViewer>

    <!-- Maintenance prompts -->
    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,20"
                  Visibility="{Binding ShowMaintenanceContent, Converter={StaticResource BoolToVisibility}}">
      <Border Style="{StaticResource Installer.CardBorder}">
        <StackPanel>
          <TextBlock Text="{Binding HeaderText}" Style="{StaticResource Installer.SectionTitle}" />
          <TextBlock Text="{Binding DescriptionText}" Style="{StaticResource Installer.BodyText}" />
        </StackPanel>
      </Border>
    </ScrollViewer>

    <!-- Progress -->
    <Border x:Name="ProgressPanel" Grid.Row="1" Padding="48,32"
            Visibility="{Binding ShowProgress, Converter={StaticResource BoolToVisibility}}">
      <Border.Style>
        <Style TargetType="Border">
          <Setter Property="Opacity" Value="1" />
          <Style.Triggers>
            <DataTrigger Binding="{Binding ShowProgress}" Value="True">
              <DataTrigger.EnterActions>
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.25" />
                  </Storyboard>
                </BeginStoryboard>
              </DataTrigger.EnterActions>
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </Border.Style>
      <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="520">
        <iconPacks:PackIconMaterial Kind="CloudDownload" Width="48" Height="48"
                                    Foreground="{DynamicResource AccentColorBrush}"
                                    HorizontalAlignment="Center" Margin="0,0,0,20" />
        <TextBlock Text="Installing..." FontSize="18" FontWeight="SemiBold"
                   HorizontalAlignment="Center"
                   Foreground="{StaticResource Installer.SectionForegroundBrush}" Margin="0,0,0,8" />
        <TextBlock Text="{Binding CurrentActionText}" Style="{StaticResource Installer.BodyText}"
                   TextAlignment="Center" Margin="0,0,0,20" MaxWidth="480" />
        <mah:MetroProgressBar Height="6" Minimum="0" Maximum="100"
                              Value="{Binding ProgressValue}" Margin="0,0,0,8" />
        <TextBlock Text="{Binding ProgressText}" HorizontalAlignment="Center"
                   FontSize="13" Foreground="{StaticResource Installer.MutedTextBrush}" />
      </StackPanel>
    </Border>

    <!-- Finish -->
    <Border x:Name="FinishPanel" Grid.Row="1" Padding="48,32"
            Visibility="{Binding ShowFinishState, Converter={StaticResource BoolToVisibility}}">
      <Border.Style>
        <Style TargetType="Border">
          <Setter Property="Opacity" Value="0" />
          <Style.Triggers>
            <DataTrigger Binding="{Binding ShowFinishState}" Value="True">
              <DataTrigger.EnterActions>
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     To="1" Duration="0:0:0.35">
                      <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                      </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                  </Storyboard>
                </BeginStoryboard>
              </DataTrigger.EnterActions>
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </Border.Style>
      <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="480">
        <Border x:Name="FinishIconHost" Width="80" Height="80" CornerRadius="40"
                HorizontalAlignment="Center" Margin="0,0,0,24"
                RenderTransformOrigin="0.5,0.5">
          <Border.RenderTransform>
            <ScaleTransform ScaleX="1" ScaleY="1" />
          </Border.RenderTransform>
          <Border.Style>
            <Style TargetType="Border">
              <Style.Triggers>
                <DataTrigger Binding="{Binding InstallSucceeded}" Value="True">
                  <Setter Property="Background" Value="#3322C55E" />
                </DataTrigger>
                <DataTrigger Binding="{Binding InstallSucceeded}" Value="False">
                  <Setter Property="Background" Value="#33EF4444" />
                </DataTrigger>
                <DataTrigger Binding="{Binding ShowFinishState}" Value="True">
                  <DataTrigger.EnterActions>
                    <BeginStoryboard>
                      <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                         From="0.5" To="1" Duration="0:0:0.45">
                          <DoubleAnimation.EasingFunction>
                            <BackEase EasingMode="EaseOut" Amplitude="0.6" />
                          </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                         From="0.5" To="1" Duration="0:0:0.45">
                          <DoubleAnimation.EasingFunction>
                            <BackEase EasingMode="EaseOut" Amplitude="0.6" />
                          </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                      </Storyboard>
                    </BeginStoryboard>
                  </DataTrigger.EnterActions>
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </Border.Style>
          <Grid>
            <iconPacks:PackIconMaterial Kind="CheckCircle" Width="44" Height="44"
                                        Foreground="{StaticResource Installer.SuccessBrush}"
                                        HorizontalAlignment="Center" VerticalAlignment="Center">
              <iconPacks:PackIconMaterial.Style>
                <Style TargetType="iconPacks:PackIconMaterial">
                  <Setter Property="Visibility" Value="Collapsed" />
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding InstallSucceeded}" Value="True">
                      <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </iconPacks:PackIconMaterial.Style>
            </iconPacks:PackIconMaterial>
            <iconPacks:PackIconMaterial Kind="CloseCircle" Width="44" Height="44"
                                        Foreground="{StaticResource Installer.ErrorBrush}"
                                        HorizontalAlignment="Center" VerticalAlignment="Center">
              <iconPacks:PackIconMaterial.Style>
                <Style TargetType="iconPacks:PackIconMaterial">
                  <Setter Property="Visibility" Value="Collapsed" />
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding InstallSucceeded}" Value="False">
                      <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </iconPacks:PackIconMaterial.Style>
            </iconPacks:PackIconMaterial>
          </Grid>
        </Border>
        <TextBlock Text="{Binding FinishTitle}" FontSize="20" FontWeight="SemiBold"
                   HorizontalAlignment="Center"
                   Foreground="{StaticResource Installer.SectionForegroundBrush}"
                   TextAlignment="Center" Margin="0,0,0,8" />
        <TextBlock Text="{Binding FinishMessage}" Style="{StaticResource Installer.BodyText}"
                   TextAlignment="Center" />
      </StackPanel>
    </Border>

    <!-- Action footer -->
    <Border Grid.Row="2" Background="{StaticResource Installer.FooterBackgroundBrush}"
            BorderBrush="{StaticResource Installer.CardBorderBrush}" BorderThickness="0,1,0,0"
            Padding="24,14"
            Visibility="{Binding ShowPrimaryContent, Converter={StaticResource BoolToVisibility}}">
      <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
          <Button Content="Cancel" Style="{StaticResource Installer.SecondaryButton}"
                  Visibility="{Binding ShowCancelButton, Converter={StaticResource BoolToVisibility}}"
                  IsEnabled="{Binding IsCancelEnabled}"
                  Command="{Binding CancelCommand}" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <StackPanel Orientation="Horizontal"
                      Visibility="{Binding ShowFreshInstallActions, Converter={StaticResource BoolToVisibility}}">
            <Button Style="{StaticResource Installer.PrimaryButton}"
                    Command="{Binding InstallCommand}">
              <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconMaterial Kind="Download" Width="16" Height="16"
                                            VerticalAlignment="Center" Margin="0,0,8,0" />
                <TextBlock Text="Install" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
          </StackPanel>

          <StackPanel Orientation="Horizontal"
                      Visibility="{Binding ShowMaintenanceActions, Converter={StaticResource BoolToVisibility}}">
            <Button Style="{StaticResource Installer.SecondaryButton}" Margin="0,0,8,0"
                    Command="{Binding RepairCommand}">
              <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconMaterial Kind="Wrench" Width="16" Height="16"
                                            VerticalAlignment="Center" Margin="0,0,6,0" />
                <TextBlock Text="Repair" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
            <Button Style="{StaticResource Installer.SecondaryButton}" Margin="0,0,8,0"
                    Command="{Binding EnterModifyCommand}">
              <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconMaterial Kind="Tune" Width="16" Height="16"
                                            VerticalAlignment="Center" Margin="0,0,6,0" />
                <TextBlock Text="Modify" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
            <Button Style="{StaticResource Installer.SecondaryButton}"
                    Command="{Binding RemoveCommand}">
              <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconMaterial Kind="DeleteOutline" Width="16" Height="16"
                                            VerticalAlignment="Center" Margin="0,0,6,0" />
                <TextBlock Text="Remove" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
          </StackPanel>

          <StackPanel Orientation="Horizontal"
                      Visibility="{Binding ShowModifyActions, Converter={StaticResource BoolToVisibility}}">
            <Button Content="Back" Style="{StaticResource Installer.SecondaryButton}" Margin="0,0,8,0"
                    Command="{Binding BackCommand}" />
            <Button Style="{StaticResource Installer.PrimaryButton}"
                    Command="{Binding ApplyModifyCommand}">
              <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconMaterial Kind="Check" Width="16" Height="16"
                                            VerticalAlignment="Center" Margin="0,0,8,0" />
                <TextBlock Text="Apply changes" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
          </StackPanel>

          <StackPanel Orientation="Horizontal"
                      Visibility="{Binding ShowUninstallConfirm, Converter={StaticResource BoolToVisibility}}">
            <Button Style="{StaticResource Installer.PrimaryButton}"
                    Command="{Binding RemoveCommand}">
              <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconMaterial Kind="DeleteForever" Width="16" Height="16"
                                            VerticalAlignment="Center" Margin="0,0,8,0" />
                <TextBlock Text="Remove" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
          </StackPanel>
        </StackPanel>
      </Grid>
    </Border>

    <!-- Complete footer -->
    <Border Grid.Row="3" Background="{StaticResource Installer.FooterBackgroundBrush}"
            BorderBrush="{StaticResource Installer.CardBorderBrush}" BorderThickness="0,1,0,0"
            Padding="24,14"
            Visibility="{Binding ShowExitButton, Converter={StaticResource BoolToVisibility}}">
      <Button Content="Close" Style="{StaticResource Installer.PrimaryButton}"
              HorizontalAlignment="Right" Command="{Binding ExitCommand}" />
    </Border>
  </Grid>
</mah:MetroWindow>
